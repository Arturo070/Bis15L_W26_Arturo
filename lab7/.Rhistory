library("tidyverse")
library("janitor")
elephants<-read_csv("data/elephants.csv")
elephants_c<-elephants %>%
clean_names()
glimpse(elephants_c)
elephants_f <- elephants_c %>%
mutate(years_of_sample_collection=as.factor
(years_of_sample_collection),
elephant_id=as.factor(elephant_id),
sex=as.factor(sex))
elephants_f %>%
ggplot(mapping =  aes(x=years_of_sample_collection)) +
geom_bar()
elephants_f %>%
count(sex)
elephants_f %>%
filter(year == "1966-68") %>%
group_by(sex)%>%
summarize(mean_sex=mean(age, na.rm=T),
median_sex=min(age, na.rm=T),
std_sex=max(age, na.rm=T))
elephants_f %>%
filter(year == "1966-68") %>%
group_by(sex)%>%
summarize(mean_sex=mean(age, na.rm=T),
median_sex=median(age, na.rm=T),
std_sex=sd(age, na.rm=T))
elephants_f %>%
filter(year == "1966-68") %>%
group_by(sex)%>%
summarize(mean_age=mean(age, na.rm=T),
median_age=median(age, na.rm=T),
std_age=sd(age, na.rm=T))
elephants_f %>%
filter(year == "1966-68") %>%
group_by(sex)%>%
summarize(mean_age=mean(age, na.rm=T),
median_age=median(age, na.rm=T),
std_age=sd(age, na.rm=T)), n=n()
elephants_f %>%
filter(year == "1966-68") %>%
group_by(sex)%>%
summarize(mean_age=mean(age, na.rm=T),
median_age=median(age, na.rm=T),
std_age=sd(age, na.rm=T)) n=n()
elephants_f %>%
filter(year == "1966-68") %>%
group_by(sex)%>%
summarize(mean_age=mean(age, na.rm=T),
median_age=median(age, na.rm=T),
std_age=sd(age, na.rm=T)), n=n())
elephants_f %>%
filter(years_of_sample_collection, sex) %>%
group_by(sex)%>%
summarize(mean_age=mean(age, na.rm=T),
median_age=median(age, na.rm=T),
std_age=sd(age, na.rm=T)), n=n())
elephants_f %>%
filter(years_of_sample_collection, sex) %>%
group_by(sex)%>%
summarize(mean_age=mean(age, na.rm=T),
median_age=median(age, na.rm=T),
std_age=sd(age, na.rm=T))n=n())
elephants_f %>%
filter(years_of_sample_collection, sex) %>%
group_by(sex)%>%
summarize(mean_age=mean(age, na.rm=T),
median_age=median(age, na.rm=T),
std_age=sd(age, na.rm=T))n=n())
elephants_f %>%
group_by(years_of_sample_collection, sex) %>%
summarize(
mean_age   = mean(age, na.rm = TRUE),
median_age = median(age, na.rm = TRUE),
sd_age     = sd(age, na.rm = TRUE),
n          = n()
)
elephants_f %>%
filter(years_of_sample_collection, sex) %>%
group_by(sex)%>%
summarize(mean_n=mean(n, na.rm=T),
median_n=median(n, na.rm=T),
std_n=sd(n, na.rm=T))n=n())
names(elephants_f)
ggplot(elephants_f, aes(x = estimated_age_years, y = tusk_length_in_cm)) +
geom_point(alpha = 0.6) +
geom_smooth(method = "lm", se = TRUE)
#elephants_f %>%
# filter(years_of_sample_collection, sex) %>%
# group_by(sex)%>%
# summarize(mean_age=mean(estimated_age_years, na.rm=T),
#           median_age=median(estimated_age_years, na.rm=T),
#          std_age=sd(estimated_age_years, na.rm=T))n=n())
elephants_f %>%
filter(years_of_sample_collection, sex) %>%
group_by(sex)%>%
summarize(mean_age=mean(estimated_age_years, na.rm=T),
median_age=median(estimated_age_years, na.rm=T),
std_age=sd(estimated_age_years, na.rm=T))n=n())
ggplot(elephants_f, aes(x = estimated_age_years, y = tusk_length_in_cm)) +
geom_point() +
geom_smooth(method = "lm", se = TRUE)
ggplot(elephants_f, aes(x = estimated_age_years, y = tusk_length_in_cm)) +
geom_point() +
geom_smooth(method = "lm")
ggplot(elephants_f, aes(x = shoulder_height_in_cm, y = tusk_length_in_cm)) +
geom_point() +
geom_smooth(method = "lm") +
ggplot(elephants_f, aes(x = shoulder_height_in_cm, y = tusk_length_in_cm)) +
geom_point() +
geom_smooth(method = "lm")
names(elephants_f)
ggplot(elephants_f, aes(x = years_of_sample_collection, y = tusk_length_in_cm)) +
geom_boxplot()
ggplot(elephants_mature,
aes(x = years_of_sample_collection, y = tusk_length_in_cm)) +
geom_boxplot()
elephants_m<-elephants_f %>%
filter((sex=="m" & estimated_age_years>=25)|(sex=="f" & estimated_age_years>=12))
ggplot(elephants_m,
aes(x = years_of_sample_collection, y = tusk_length_in_cm)) +
geom_boxplot()
